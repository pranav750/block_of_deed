import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles(() => ({
  root: {
    width: "40%",
    margin: "30px auto",
    padding: "30px",
    textAlign: "center",
  },

  title: {
    fontWeight: "700",
  },

  extraMargin: {
    margin: "10px",
  },
}));

export default useStyles;
